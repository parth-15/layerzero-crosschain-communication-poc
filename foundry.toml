[profile.default]
src = "src"
out = "out"
libs = ["lib"]

[rpc_endpoints]
polygon = "${POLYGON_URL}"
[etherscan]
polygon = { key = "${ETHERSCAN_API_KEY}", chain=127 }

# See more config options https://github.com/foundry-rs/foundry/tree/master/config